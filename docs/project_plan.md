# AI 秋招岗位匹配与简历优化助手：项目计划书

## 项目概述

本项目是一个面向秋招候选人的网页版求职辅助工具。用户输入简历和岗位 JD 后，系统通过多 Agent 工作流完成岗位解析、简历解析、证据匹配、关键词覆盖、匹配评分、简历优化、投递话术生成和报告导出。

MVP 使用 Python + Streamlit 实现，默认支持 mock/规则模式，因此没有 API Key 也可以本地运行和作品集演示；当配置 OpenAI API Key 后，可以切换为 LLM 增强模式。

## 背景与痛点

秋招求职通常不是“写一份简历投所有岗位”，而是需要针对不同 JD 快速判断匹配度并调整表达。典型痛点包括：

- JD 长且复杂，岗位职责、任职要求、业务场景和加分项混在一起。
- 简历经历与岗位要求之间缺少明确证据映射。
- 关键词覆盖不足，导致初筛系统或招聘方快速浏览时无法识别匹配点。
- 候选人难以判断当前简历更适合哪个岗位方向。
- 批量投递时，邮件、Boss 直聘、LinkedIn 和内推话术需要重复手写，效率低。
- LLM 直接改简历容易出现编造经历风险，需要明确约束。

## 目标用户

- 2026/2027 届秋招学生。
- 转向 AI 产品、数据分析、商业分析、数据产品、AI Agent 产品等方向的候选人。
- 需要批量投递并快速做岗位适配的人。
- 想把简历优化过程产品化、结构化、可解释化的求职者。

## 核心功能

1. 简历输入：支持文本粘贴和 `.txt` / `.pdf` / `.docx` 上传。
2. JD 输入：支持岗位 JD 粘贴和目标岗位方向填写。
3. JD 解析：抽取岗位名称、公司、地点、职责、技能、工具、业务关键词和隐含能力。
4. 简历解析：抽取教育、项目、实习、技能、指标、关键词和可迁移能力。
5. 证据匹配：输出“岗位要求 - 简历证据 - 证据强度 - 建议补充表达”。
6. 关键词覆盖：输出已覆盖、弱覆盖和未覆盖关键词。
7. 匹配评分：按固定权重生成可解释 0-100 分。
8. 简历优化：基于原始经历给出 bullet 改写建议，不编造内容。
9. 投递话术：生成 Boss、邮件、LinkedIn、内推和面试自我介绍。
10. 报告导出：支持 Markdown 和 Word `.docx` 报告。

## Agent 工作流

项目采用模块化 Agent 编排：

1. Input Router 接收简历文本、上传文件解析结果、JD 和目标岗位方向。
2. JD 解析 Agent 将非结构化 JD 转为 `JDAnalysis`。
3. 简历解析 Agent 将非结构化简历转为 `ResumeAnalysis`。
4. 简历证据提取 Agent 映射 JD 要求和简历证据。
5. 关键词覆盖 Agent 计算关键词覆盖情况。
6. 匹配评分 Agent 按权重计算总分和分项原因。
7. 简历优化 Agent 输出 bullet 改写建议。
8. 话术生成 Agent 输出多渠道投递表达。
9. 最终报告 Agent 汇总为 Markdown 报告。
10. 报告导出模块将结构化结果转换为 Word 文档。

## 技术架构

- 前端和交互：Streamlit。
- 后端语言：Python。
- 数据模型：Pydantic。
- 文档解析：`pypdf`、`python-docx`、文本解码。
- LLM 适配：OpenAI API 封装在 `src/llm_client.py`。
- Mock 模式：本地规则、关键词库和启发式评分。
- 报告导出：Markdown 原文下载和 `python-docx` 生成 Word 报告。
- 测试：pytest。

## 数据输入输出

输入：

- 简历文件：`.txt`、`.pdf`、`.docx`。
- 简历文本：用户直接粘贴。
- 岗位 JD：用户粘贴。
- 目标岗位方向：例如 AI 产品经理、数据分析师、商业分析师、数据产品经理、AI Agent 产品经理。

输出：

- JD 解析结果。
- 简历证据匹配表。
- 关键词覆盖分析。
- 匹配评分与解释。
- 简历优化建议。
- 投递话术。
- 最终 Markdown 报告。
- Word `.docx` 报告。

## MVP 范围

MVP 优先保证可运行、可演示、可上传 GitHub：

- 单个简历与单个 JD 的匹配分析。
- 本地 mock 模式完整闭环。
- OpenAI API 可选增强。
- Markdown/表格形式展示，不追求复杂图表。
- Word 报告导出。
- 基础 pytest 自动测试。

暂不包含：

- 批量岗位排序。
- 简历版本管理。
- 在线用户账号。
- 数据库持久化。
- PDF 导出和复杂排版。

## 测试方案

- 单元/集成测试：`pytest` 验证 mock 模式端到端工作流和 Word 导出。
- 手动测试：覆盖文本输入、文件上传、缺失输入、不同岗位 JD、OpenAI/mock 模式切换。
- UI 验证：启动 Streamlit 并访问本地地址，确认页面返回 HTTP 200。
- 安全检查：确认 `.env` 被忽略，`.env.example` 不含真实 Key。

## 风险与解决方案

| 风险 | 影响 | 解决方案 |
| --- | --- | --- |
| LLM 输出 JSON 不稳定 | 结构化解析失败 | 统一封装 `LLMClient`，失败时回退本地规则 |
| 无 API Key 无法演示 | 作品集体验受限 | 默认 mock 模式完整运行 |
| 简历优化出现编造 | 求职风险高 | Agent 提示词和 UI 明确“不编造”原则 |
| PDF/DOCX 解析质量不稳定 | 简历文本可能缺失 | 支持粘贴文本作为兜底 |
| 评分过度精确 | 用户误解结果 | 输出分项原因和风险提示 |
| 关键词库不够完整 | 覆盖分析偏粗 | 后续增加岗位方向专属关键词库 |

## 项目亮点

- 把求职简历优化拆成可解释的多 Agent 工作流。
- Mock 模式可独立演示，适合作品集和 GitHub 展示。
- 评分逻辑透明，权重清晰。
- 支持简历证据表，帮助用户知道“哪里匹配、哪里缺证据”。
- 报告可下载，方便保存、复盘和分享。
- 明确防止 LLM 编造经历，符合真实求职场景。

## 可展示成果

- 可运行 Streamlit 网页。
- 完整 GitHub 项目结构。
- 示例简历和示例 JD。
- Agent 工作流 Mermaid 图。
- Markdown 和 Word 报告导出。
- 测试用例文档和自动测试。
- 可放入作品集的项目计划书与 README。

## 简历项目描述

AI 秋招岗位匹配与简历优化助手：基于 Python、Streamlit 和多 Agent 工作流构建求职辅助 MVP，支持简历上传/粘贴、JD 解析、简历证据匹配、关键词覆盖分析、可解释匹配评分、简历 bullet 优化、多渠道投递话术生成和 Markdown/Word 报告导出；封装 OpenAI API 并提供无 Key 可运行的 mock 模式，使用 Pydantic 管理结构化输出，pytest 覆盖端到端工作流和报告导出。
